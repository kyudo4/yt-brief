# Skopiuj do .env i uzupełnij. .env jest w .gitignore — nigdy nie commituj kluczy.

# YouTube Data API v3 — https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=

# Google Gemini (LLM) — darmowy tier: https://aistudio.google.com/apikey
# Uwaga: na darmowym tierze dane z zapytań mogą iść na trening Google.
GEMINI_API_KEY=
# GitHub Models (drafty na GPT-4.1) — w Actions używa wbudowanego GITHUB_TOKEN;
# lokalnie opcjonalnie: GH_MODELS_TOKEN=<token z uprawnieniem models:read>
# Łańcuchy failoveru (opcjonalne; przecinkami, prefix "github:" = GitHub Models):
# LLM_CHAIN_DRAFTS=github:openai/gpt-4.1,github:openai/gpt-4o,gemini-2.5-flash
# LLM_CHAIN_TOPICS=gemini-2.5-flash,gemini-2.5-flash-lite
# LLM_CHAIN_EXTRACT=gemini-2.5-flash-lite,gemini-2.5-flash

# Telegram — token od @BotFather, chat_id np. z @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Publiczny URL strony (GitHub Pages), używany w wiadomości Telegram
SITE_URL=https://TWOJ-LOGIN.github.io/yt-brief

# Proxy rezydencjalny do transkrypcji — POTRZEBNY na GitHub Actions (chmura),
# bo YouTube blokuje pobieranie napisów z datacenter IP. Lokalnie NIEpotrzebny.
# Webshare: https://www.webshare.io -> Residential proxy -> Proxy Settings -> username/password.
WEBSHARE_PROXY_USERNAME=
WEBSHARE_PROXY_PASSWORD=
# Alternatywnie dowolny inny proxy (zamiast Webshare):
# YT_PROXY_URL=http://user:pass@host:port
